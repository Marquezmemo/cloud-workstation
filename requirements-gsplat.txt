# Core gsplat runtime support.
# The CUDA wheel stays on the Phase 4 validated PyPI release. The official
# examples are cloned separately at a pinned git commit.
gsplat==1.5.3
jaxtyping==0.3.11
ninja==1.13.0
numpy==1.26.3
rich==14.3.4
typing_extensions==4.15.0
wadler-lindig==0.1.7
