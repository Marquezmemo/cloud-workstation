# The Trainer v0.1 Final Operator Attestation

Declaration recorded: `2026-07-04`

Exact execution date: `not-preserved`

```text
validation_status: passed
validation_authority: repository-owner-operator
evidence_level: operator-confirmed
primary_execution_records: not-preserved
```

## Scope

The repository owner and execution operator confirms that the image corresponding to commit `221c4ef76a1ff5bfa8a9f45e5e9a084e8fbedfe3` and digest `sha256:726ac98c9678431fc34fbcc1de0bf07b71adbff215d672f04740a95999a6bf98` completed the following real flow with scene `Prueba02`:

```text
real Surveyor scene archive
-> transfer
-> checksum verification
-> preparar-escena
-> transactional scene installation
-> train-scene.sh --check
-> CUDA training
-> checkpoint
-> PLY generation
-> packaging
-> transfer
-> PLY viewer load
```

## Attested Results

| Field | Result |
| --- | --- |
| Scene | `Prueba02` |
| Commit | `221c4ef76a1ff5bfa8a9f45e5e9a084e8fbedfe3` |
| Image digest | `sha256:726ac98c9678431fc34fbcc1de0bf07b71adbff215d672f04740a95999a6bf98` |
| Overall result | `passed` |
| Scene archive transfer | `passed` |
| Checksum | `passed` |
| `preparar-escena` | `passed` |
| Transactional installation | `passed` |
| `train-scene.sh --check` | `passed` |
| CUDA training | `passed` |
| Checkpoint | `generated` |
| PLY | `generated` |
| Packaging | `passed` |
| Result transfer | `passed` |
| Viewer load | `passed` |
| Primary logs and transcript | `not-preserved` |
| Primary checkpoint and artifacts | `not-preserved` |
| Professional or commercial quality | `not-evaluated` |
| Densification | `not-certified-by-this-attestation` |

## Evidence Classification

This declaration is an operator attestation. It is not classified as `validated with preserved evidence` or `validated with evidence` because the primary logs, transcript, checkpoint, transferred packages, and generated PLY from this execution were not preserved.

No execution date, duration, training depth, performance metric, file hash, file size, image count, Gaussian count, or quality metric is inferred for this run.

## Relationship To The Original Prueba02 Record

The [original Prueba02 validation](2026-06-29-prueba02-trainer-e2e.md) remains a separate earlier execution on an earlier runtime. It preserves primary evidence and records manual archive extraction before `train-scene.sh --check`.

This final v0.1 attestation covers the later runtime at commit `221c4ef76a1ff5bfa8a9f45e5e9a084e8fbedfe3`, including automatic, transactional scene preparation through `preparar-escena`. Its authority is the repository owner/operator and its primary execution records were not preserved.
